from class_pessoa.py import Pessoa

class SalaDeAula

        def __init__(self, numero: int, capacidade: int):
            self.numero = numero
            self.capacidade = capacidade
            print(f"Sala {self.numero} está disponivel.")

            class Professor(Pessoa):
                self.disciplina
        def dar_aula(self, sala: SalaDeAula):
            print(f"O Prof. {self.nome} de {self.disciplina} está dando aula na sala{sala.numero}.")    
        pessoa = Professor()
        professor = dar_aula    